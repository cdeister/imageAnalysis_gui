function out=inferRunFromRegistration(transforms,smoothValue)

if nargin==1
    sV=20;
else
    sV=smoothValue;
end
    putRun=abs(diff(transforms));
    putRun=[putRun(:,1),putRun];
    out=smooth(putRun,sV);

end